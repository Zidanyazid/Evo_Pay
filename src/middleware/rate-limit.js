const windows=new Map();
export function rateLimit({limit=120,windowMs=60000,key=(req)=>req.merchant?.id||req.ip}={}){return(req,res,next)=>{const k=key(req);const t=Date.now();let item=windows.get(k);if(!item||item.reset<=t)item={count:0,reset:t+windowMs};item.count++;windows.set(k,item);res.set({'X-RateLimit-Limit':String(limit),'X-RateLimit-Remaining':String(Math.max(0,limit-item.count)),'X-RateLimit-Reset':String(Math.ceil(item.reset/1000))});if(item.count>limit)return res.status(429).json({error:{code:'RATE_LIMITED',message:'Terlalu banyak request. Coba kembali sebentar lagi.'}});next()}}
