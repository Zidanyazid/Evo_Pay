import {Router} from 'express';import {authorize} from '../middleware/authorization.js';import {requirePermission} from '../middleware/permissions.js';
export default function billingFinanceRoutes(service,jobs){const router=Router(),auth=authorize();router.use(auth);router.get('/recurring/plans',requirePermission('billing:read'),(q,s)=>s.json({data:service.plans(q.query.merchant_id||'m_demo_topup')}));router.post('/recurring/plans',requirePermission('billing:write'),(q,s,n)=>{try{s.status(201).json({data:service.createPlan(q.body.merchant_id||'m_demo_topup',q.body)})}catch(e){n(e)}});router.get('/recurring/subscriptions',requirePermission('billing:read'),(q,s)=>s.json({data:service.subscriptions(q.query.merchant_id||'m_demo_topup')}));router.post('/recurring/subscriptions',requirePermission('billing:write'),(q,s,n)=>{try{s.status(201).json({data:service.subscribe(q.body.merchant_id||'m_demo_topup',q.body,jobs)})}catch(e){n(e)}});router.post('/recurring/subscriptions/:id/:action',requirePermission('billing:write'),(q,s,n)=>{try{s.json({data:service.transition(q.params.id,q.params.action)})}catch(e){n(e)}});router.get('/reports',requirePermission('reports:read'),(q,s)=>s.json({data:service.reports(q.admin.user_id)}));router.post('/reports',requirePermission('reports:write'),(q,s,n)=>{try{s.status(201).json({data:service.createReport(q.admin.user_id,q.body)})}catch(e){n(e)}});router.post('/reports/:id/run',requirePermission('reports:read'),(q,s,n)=>{try{s.json({data:service.runReport(q.params.id)})}catch(e){n(e)}});router.post('/reconciliation/:provider/import',requirePermission('reconciliation:write'),(q,s,n)=>{try{s.status(201).json({data:service.reconcile(q.params.provider,q.body)})}catch(e){n(e)}});router.get('/forecast/:merchantId',requirePermission('reports:read'),(q,s)=>s.json({data:service.forecast(q.params.merchantId,Math.min(365,Number(q.query.days)||30))}));return router}
