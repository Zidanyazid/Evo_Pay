import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const provider=fs.readFileSync(new URL('../src/services/provider-service.js',import.meta.url),'utf8'),routing=fs.readFileSync(new URL('../src/services/routing-service.js',import.meta.url),'utf8'),operations=fs.readFileSync(new URL('../src/services/operations-service.js',import.meta.url),'utf8'),routes=fs.readFileSync(new URL('../src/routes/operations-routes.js',import.meta.url),'utf8'),html=fs.readFileSync(new URL('../public/index.html',import.meta.url),'utf8');
test('provider health persists circuit policy and transitions',()=>{assert.match(provider,/failure_threshold/);assert.match(provider,/recovery_seconds/);assert.match(provider,/circuit_state='OPEN'/);assert.match(provider,/circuit_state='HALF_OPEN'/);assert.match(provider,/circuit_state='CLOSED'/);});
test('routing excludes providers with an open persistent circuit',()=>{assert.match(routing,/provider_circuit_policies/);assert.match(routing,/circuit_state!=='OPEN'/);});
test('health scoring exposes samples latency failures and degraded state',()=>{for(const term of ['success_rate','latency_ms','samples','failures','DEGRADED'])assert.match(provider,new RegExp(term));});
test('incidents support public communication and status endpoint',()=>{assert.match(operations,/public_message/);assert.match(operations,/public_status/);assert.match(routes,/\/status/);assert.match(routes,/filter\(x=>x\.public_status\)/);});
test('routing center exposes circuit policy and incident forms',()=>{for(const id of ['circuit-policy-form','provider-incident-list','incident-form'])assert.match(html,new RegExp(`id="${id}"`));});
