import test from 'node:test';
import assert from 'node:assert/strict';
import crypto from 'node:crypto';
import { TokopayProvider } from '../src/providers/tokopay-provider.js';
import { publicPayment } from '../src/services/payment-service.js';
test('Tokopay accepts only a valid reference signature', () => { const provider = new TokopayProvider({ merchantId: 'merchant', secret: 'secret' }); const ref = 'reference-1'; const signature = crypto.createHash('md5').update(`merchant:secret:${ref}`).digest('hex'); assert.equal(provider.verifyWebhook({ reff_id: ref, signature }), true); assert.equal(provider.verifyWebhook({ reff_id: ref, signature: 'bad' }), false); });
test('public payment never exposes Site API key or callback secret', () => { const data = publicPayment({ id: 'pay_1', site_id: 'site_1', order_id: 'order', provider_reference: 'ref', status: 'PENDING', payment_method: 'QRIS', amount: 1000, created_at: 'now', updated_at: 'now' }); assert.deepEqual(Object.keys(data).filter((key) => /key|secret/i.test(key)), []); });
import { responsePreview, retryState } from '../src/services/webhook-service.js';
test('webhook response preview strips controls and limits stored content', () => { assert.equal(responsePreview(`ok\u0000${'x'.repeat(2000)}`).length, 1024); assert.equal(responsePreview('a\u0000b'), 'ab'); });
test('webhook retry state reaches a terminal dead letter', () => { assert.equal(retryState(1), 'RETRYING'); assert.equal(retryState(999), 'DEAD_LETTER'); });
import { reconcileOutcome } from '../src/services/reconciliation-service.js';
test('reconciliation classifies unchanged and corrected statuses', () => { assert.equal(reconcileOutcome('PENDING', 'PENDING'), 'MATCHED'); assert.equal(reconcileOutcome('PENDING', 'PAID'), 'FIXED'); });
import { auditSummary, safeMetadata } from '../src/services/audit-service.js';
test('audit metadata excludes secrets and remains valid bounded JSON', () => { const metadata = safeMetadata({ name: 'Project', api_key: 'ep_should_not_store', webhook_secret: 'should_not_store', detail: 'x'.repeat(2000) }); assert.equal(metadata.includes('should_not_store'), false); assert.ok(metadata.length <= 1024); assert.equal(auditSummary({ metadata_json: metadata }).includes('Project'), true); });
import { buildSeries, paymentFilters, successRate, validDays } from '../src/services/dashboard-service.js';
test('dashboard analytics calculates rate and fills empty days', () => { assert.equal(successRate({ paid: 8, failed: 1, expired: 1 }), 80); assert.equal(validDays(999), 7); const series = buildSeries([{ day: '2026-08-24', transactions: 2, paid_amount: 10000 }], 3, new Date('2026-08-25T12:00:00Z')); assert.equal(series.length, 3); assert.equal(series[0].transactions, 0); assert.equal(series[1].paid_amount, 10000); });
test('dashboard payment filters reject inverted dates and unknown status', () => { assert.throws(() => paymentFilters({ from: '2026-08-25', to: '2026-08-24' })); assert.equal(paymentFilters({ status: 'NOT_A_STATUS' }).status, null); });
import { EvoPayClient, EvoPayError } from '../examples/node/evopay-client.js';
import { requestId, validRequestId } from '../src/request-id.js';
test('request ID accepts only bounded safe values', () => { assert.equal(validRequestId('dev_request-123'), true); assert.equal(validRequestId('bad value'), false); assert.match(requestId('bad value'), /^[0-9a-f-]{36}$/); });
test('Node helper maps API errors with request ID', async () => { const client = new EvoPayClient({ baseUrl: 'http://gateway.test', apiKey: 'ep_test', fetchFn: async () => new Response(JSON.stringify({ error: { code: 'NOT_FOUND', message: 'Tidak ada' } }), { status: 404, headers: { 'x-request-id': 'dev_request-123' } }) }); await assert.rejects(() => client.getPayment('pay_missing'), (error) => error instanceof EvoPayError && error.status === 404 && error.code === 'NOT_FOUND' && error.requestId === 'dev_request-123'); });
import { metrics, sanitize } from '../src/observability.js';
test('observability redacts nested secrets and renders bounded metrics', () => { assert.equal(sanitize({ nested: { api_key: 'ep_secret' } }).nested.api_key, '[REDACTED]'); metrics.reset(); metrics.increment('evopay_test_total', { outcome: 'ok' }); assert.match(metrics.render({ evopay_test_gauge: 2 }), /evopay_test_total\{outcome="ok"\} 1/); assert.match(metrics.render({ evopay_test_gauge: 2 }), /evopay_test_gauge 2/); });
import { sandboxPayment, siteMode } from '../src/services/sandbox-payment-service.js';
test('sandbox payment stays local and modes default to LIVE', () => { assert.equal(siteMode('unknown'), 'LIVE'); const payment = sandboxPayment({ reference: 'EPTEST001', amount: 15000 }); assert.equal(payment.raw.simulator, true); assert.match(payment.paymentUrl, /^sandbox:/); assert.equal(payment.paymentCode.startsWith('TEST-'), true); });
import { testPayload } from '../src/services/webhook-test-service.js';
test('webhook test payload is explicitly synthetic and excludes customer PII', () => { const payload = testPayload({ id: 'site_test' }); assert.equal(payload.event, 'payment.paid'); assert.equal(payload.test, true); assert.equal(payload.data.environment, 'TEST'); assert.equal(payload.data.amount, 1000); assert.equal('customer' in payload.data, false); });
import { expirationEligible } from '../src/services/payment-expiration-service.js';
test('expiration worker only accepts overdue LIVE pending payments', () => { const past = new Date(Date.now() - 1000).toISOString(), future = new Date(Date.now() + 10000).toISOString(); assert.equal(expirationEligible({ environment: 'LIVE', status: 'PENDING', expires_at: past }), true); assert.equal(expirationEligible({ environment: 'TEST', status: 'PENDING', expires_at: past }), false); assert.equal(expirationEligible({ environment: 'LIVE', status: 'PAID', expires_at: past }), false); assert.equal(expirationEligible({ environment: 'LIVE', status: 'PENDING', expires_at: future }), false); assert.equal(expirationEligible({ environment: 'LIVE', status: 'PENDING' }), false); });
import { maintenanceError } from '../src/middleware/site-maintenance.js';
test('maintenance mode returns a safe bounded create-payment response', () => { const error = maintenanceError({ id: 'site_private', maintenance_message: 'internal detail' }); assert.deepEqual(error, { code: 'SITE_MAINTENANCE', message: 'Pembayaran sementara tidak tersedia untuk Site ini.', retry_after_seconds: 300 }); assert.equal(JSON.stringify(error).includes('internal'), false); });
import { effectiveLimit, rateHeaders, validateLimit, windowStart } from '../src/services/site-rate-limit-service.js';
test('Site rate limit helpers isolate buckets and emit standard headers', () => { const start = windowStart(120001); assert.equal(start, 120000); assert.equal(validateLimit(null), true); assert.equal(validateLimit(1), true); assert.equal(validateLimit(0), false); assert.equal(effectiveLimit({ create_payment_limit_per_minute: 4 }, 'CREATE_PAYMENT', { CREATE_PAYMENT: 30, SYNC_PAYMENT: 60 }), 4); assert.equal(effectiveLimit({}, 'SYNC_PAYMENT', { CREATE_PAYMENT: 30, SYNC_PAYMENT: 60 }), 60); const headers = rateHeaders({ limit: 30, remaining: 0, reset: Date.now() + 5000 }); assert.equal(headers['X-RateLimit-Limit'], '30'); assert.equal(Number(headers['Retry-After']) >= 1, true); });
import fs from 'node:fs';
test('recovery runbook includes safety-critical recovery sections', () => { const runbook = fs.readFileSync(new URL('../docs/RUNBOOK.md', import.meta.url), 'utf8'); for (const section of ['## Callback Site gagal', '## Tokopay lambat atau tidak tersedia', '## Database down', '## Backup dan restore database', '## Rotasi credential']) assert.equal(runbook.includes(section), true); assert.equal(runbook.includes('Jangan restore di atas database produksi aktif'), true); });
import {ratePressure,reconciliationStale} from '../src/services/alert-service.js';
test('alert helpers use exact pressure and stale-work boundaries',()=>{assert.equal(ratePressure(8,10),true);assert.equal(ratePressure(7,10),false);assert.equal(reconciliationStale(null,0),false);assert.equal(reconciliationStale({finished_at:new Date(Date.now()).toISOString()},1),false);});
